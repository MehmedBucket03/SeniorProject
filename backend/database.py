#SQLite connection

import sqlite3

#not ready yet
#DATABASE_NAME = "algomatics.db"

#    """Establishes and returns a connection to the SQLite database."""
#    conn = sqlite3.connect(DATABASE_NAME)
#    conn.row_factory = sqlite3.Row  # Allows accessing columns by name
#    return conn
